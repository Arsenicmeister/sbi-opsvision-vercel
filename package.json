{
  "name": "sbi-opsvision-vercel",
  "version": "1.0.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "pg": "^8.11.0"
  }
}
